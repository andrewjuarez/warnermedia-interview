const INITIAL_STATE = {
    results: [],
    success: null,
    loading: false,
};

const SearchReducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {
        

        default:
            return state;
    }
}

export default SearchReducer;